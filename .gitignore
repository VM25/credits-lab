# Python
__pycache__/
.pytest_cache/
*.pyc
*.pyo
.venv/
venv/
env/
*.egg-info/
.ipynb_checkpoints/

# Node / frontend
node_modules/
frontend/dist/
frontend/.vite/

# OS
.DS_Store
Thumbs.db

# Secrets (never commit credentials)
.kaggle/
kaggle.json
*.env
.env*

# Large / re-derivable raw inputs (downloaded from Kaggle/FRED; not committed)
data/raw/

# Local caches
.cache/
*.log

# Claude Code local config (not part of the project)
.claude/

# Local Netlify folder
.netlify

# package-lock omitted so CI uses npm install (fixes npm optional-dep/rollup-native bug on Linux)
frontend/package-lock.json
