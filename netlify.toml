# Netlify build config. The site lives in frontend/; its build copies the
# backend's committed data/outputs into the bundle (see frontend predev/prebuild).
[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
